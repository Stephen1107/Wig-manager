The Wig Inventory Management System is a console-based Java application designed to help salon owners or wig vendors manage their inventory efficiently. It allows users to track wigs, manage stock levels, record sales, and monitor availability.

The system is built using pure Java (no Spring Boot or external frameworks) and uses file-based storage for data persistence.
